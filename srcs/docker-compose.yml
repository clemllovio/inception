version: '3.8'

services :
  nginx:
    image: nginx
    container_name: nginx
    restart: on-failure
    build:
        context: requirements/nginx
        dockerfile: Dockerfile 
    env_file:
      - .env
    ports:
      - "443:443"
    # volume:
    #   - ./requirements/nginx/conf/nginx.conf:etc/nginxconf.d/core.conf
    # links:
    #   - mariadb

  # mariadb:
  #   image: mariadb
  #   container_name: mariadb
  #   restrat: on-failure
    
  #   volumes:
  #     - mariadb:/var/lib/mysql
  #   networks:
  #     - inception
  #   ports:
  #     - 3306:3306

# volume:
#   maridb:

# network:
#   inception